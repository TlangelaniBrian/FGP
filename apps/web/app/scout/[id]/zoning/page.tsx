import Link from "next/link";

const steps = ["Confirm current zoning certificate", "Prepare dolomite declaration", "Submit building plan checklist", "Compile pre-application motivation"];

export default async function ZoningPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  return <div className="portal-page"><div className="portal-page-head"><div><p className="eyebrow">Parcel {id} · Compliance</p><h1 className="page-title">Zoning and forms</h1><p className="page-subtitle">RES3 · City of Johannesburg · A working checklist for the next application step.</p></div><Link href={`/scout/${id}`} className="button button-quiet">← Back to parcel</Link></div><div className="grid-2"><section className="card card-pad"><div className="split"><div><span className="card-kicker">Application checklist</span><h2 className="card-title" style={{ marginTop: 6 }}>Keep the file moving</h2></div><span className="tag tag-blue">1 of 4 ready</span></div><div className="compliance-timeline">{steps.map((step, index) => <div className="compliance-step" key={step}><span className={index === 0 ? "step-dot complete" : "step-dot"}>{index === 0 ? "✓" : index + 1}</span><div><strong>{step}</strong><small>{index === 0 ? "Spatial match confirmed · Ready to download" : index === 1 ? "Required before municipal submission" : "Not started"}</small></div><span className={index === 0 ? "tag tag-green" : "tag"}>{index === 0 ? "Ready" : "Pending"}</span></div>)}</div></section><section className="stack"><section className="card card-pad"><span className="card-kicker">Zoning summary</span><h2 className="card-title" style={{ marginTop: 6 }}>Residential 3</h2><div className="list-row"><span><strong>Permitted use</strong><small>Multiple dwelling units</small></span><span className="tag tag-green">Permitted</span></div><div className="list-row"><span><strong>Rezoning</strong><small>Not required for 8 units</small></span><span className="tag tag-blue">Low friction</span></div></section><section className="card card-pad"><span className="card-kicker">Generated documents</span><h2 className="card-title" style={{ marginTop: 6 }}>Compliance package</h2><p className="muted" style={{ fontSize: 12, lineHeight: 1.5 }}>Generate a clean working pack for your planning professional once the parcel inputs are confirmed.</p><button className="button button-primary" style={{ width: "100%", marginTop: 8 }} onClick={() => undefined}>Generate package</button></section></section></div></div>;
}
